<h1>Movies List</h1>
<div class="container"></div>
  <ol>
    <% @lists.each do |list|  %>

      <li class="card">Name: <%=link_to list.name, list_path(list)%></li>

    <% end %>
  </ol>
</div>
<%= link_to "Create new List", new_list_path %>
